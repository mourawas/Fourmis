#include <iostream>
#include <vector>
#include "error_squarcell.h"
using namespace std;

class Grille {
	private:
	vector<vector<bool>> grid;
	const int g_max
	public:
	Grille(int g_max_);
	:g_max (g_max_) {}
	
	
